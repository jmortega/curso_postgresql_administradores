apiVersion: 1

providers:

  - name: "PostgreSQL Monitoring"
    orgId: 1
    folder: "PostgreSQL"
    folderUid: "postgres-monitoring"
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
