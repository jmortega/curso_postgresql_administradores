apiVersion: 1

providers:
  - name: "PostgreSQL Lab"
    orgId: 1
    folder: "PostgreSQL"
    folderUid: "postgres"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
